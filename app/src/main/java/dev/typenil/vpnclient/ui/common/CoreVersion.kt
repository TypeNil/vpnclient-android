package dev.typenil.vpnclient.ui.common

/** Pinned in gradle/libs.versions.toml (`vpnCore`) — displayed on Settings
 *  and the session-details sheet; keep in sync with the pinned version. */
const val CORE_VERSION = "sing-box libbox 1.14.1"
